# The course now includes the local Node demonstration boundary

The mission expanded to Slice 6's thin Node ESM CLI, strict two-path policy, delayed output publication, cleanup, summary formatting, and subprocess evidence. Future teaching should keep all filesystem and process behavior under `scripts/`, import only the compiled public core, and treat the final output filename as a success signal that appears only after in-memory verification.
